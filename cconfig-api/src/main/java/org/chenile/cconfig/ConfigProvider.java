package org.chenile.cconfig;

/**
 * All ConfigProviders can supply values to a key or a bunch of keys using their own
 * strategy. Config providers must register with the ConfigProvideFactory against a config type.
 */
public interface ConfigProvider {
}
